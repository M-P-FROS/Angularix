import { FigurineTypeColorPipe } from './figurine-type-color.pipe';

describe('FigurineTypeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new FigurineTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
